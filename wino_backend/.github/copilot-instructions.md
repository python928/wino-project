# Backend subfolder note

This repo is a monorepo (Django backend + Flutter app). The canonical agent guidance lives at:

- `/.github/copilot-instructions.md`

When working specifically in `wino_backend/`, follow the backend sections in the root instructions (entrypoints, URL routing, JWT auth, DB switching via `POSTGRES_*`, and the messaging/notifications route mismatch).

