// SearchTracker is no longer needed.
// Searches and filters are tracked server-side in ProductViewSet.list()
// by reading query params (?search=, ?min_price=, ?max_price=, ...).
// This file is kept as a stub so old imports don't break.
